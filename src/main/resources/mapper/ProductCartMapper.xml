<?xml version="1.0" encoding="utf-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.ag.dao.ProductCartDao">
    <select id="productQueryById" resultType="ProductInfo">
        SELECT product_id as productid ,prodcut_name as prodcutname ,product_price as productprice ,product_stock as productstock
from product_info where product_id=#{id}
    </select>

</mapper>